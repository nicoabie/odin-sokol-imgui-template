{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "odin: build debug main",
            "command": "odin",
            "args": ["build", "src/main.odin", "-file", "-debug"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
        }
    ]
}